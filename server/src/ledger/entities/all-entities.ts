import { Account } from './account.entity';
import { Journal } from './journal.entity';
import { JournalEntry } from './journal-entry.entity';

export const LedgerEntities = [Account, Journal, JournalEntry];
